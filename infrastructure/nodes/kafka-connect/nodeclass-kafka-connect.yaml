apiVersion: karpenter.k8s.aws/v1
kind: EC2NodeClass
metadata:
  name: kafka-connect
spec:
  role: kube-system-experiment-karpenter-node

  amiSelectorTerms:
    - alias: al2023@latest

  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: kube-system-experiment-eks

  securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: kube-system-experiment-eks

  blockDeviceMappings:
    - deviceName: /dev/xvda
      ebs:
        volumeSize: 100Gi
        volumeType: gp3
        encrypted: true
        deleteOnTermination: true

  tags:
    Project: kube-system-experiment
    ManagedBy: karpenter
    Workload: kafka-connect
