[project]
name = "process"
version = "0.1.0"
description = "Geracao de datasets e treino de modelos para o pipeline Spark no EKS."
readme = "README.md"
authors = [
    { name = "Manuel Ventura", email = "manuel.ventura.neto@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "scikit-learn>=1.9.0",
    "xgboost>=3.4.1",
]

[tool.uv]
package = false
